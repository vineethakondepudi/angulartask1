import { Customepipe1Pipe } from './customepipe1.pipe';

describe('Customepipe1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Customepipe1Pipe();
    expect(pipe).toBeTruthy();
  });
});
